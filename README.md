# 武陵微缩景观

基于 Three.js 的可交互武陵风格三维微缩场景。建筑、底座、水景和植被由代码实时构建，支持拖拽旋转、滚轮和双指缩放。

## 本地运行

在项目根目录运行：

```bash
python3 -m http.server 8000 --directory dist
```

然后访问 `http://localhost:8000`。场景依赖已放在 `dist/vendor/`，无须安装其他包。

[查看在线版本](https://wuling-miniature.cyclopsdot.chatgpt.site)
